---
phase: 03-type-safety-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/blog/utils/blogUtils.ts
  - src/blog/BlogPost.tsx
  - src/blog/BlogHome.tsx
  - src/server/api.ts
autonomous: true

must_haves:
  truths:
    - "BlogPost type includes primaryTag and relatedPostSlugs fields"
    - "Zero as-any casts in blogUtils.ts and BlogPost.tsx"
    - "No debug console.log in BlogHome.tsx production code"
    - "Dead src/server/api.ts file is deleted"
  artifacts:
    - path: "src/types.ts"
      provides: "Extended BlogPost interface"
      contains: "primaryTag?: string"
    - path: "src/types.ts"
      provides: "Extended BlogPost interface"
      contains: "relatedPostSlugs?: string[]"
    - path: "src/blog/utils/blogUtils.ts"
      provides: "Type-safe related posts logic"
    - path: "src/blog/BlogPost.tsx"
      provides: "Type-safe post rendering"
  key_links:
    - from: "src/blog/utils/blogUtils.ts"
      to: "src/types.ts"
      via: "BlogPost.primaryTag field access"
      pattern: "currentPost\\.primaryTag"
    - from: "src/blog/BlogPost.tsx"
      to: "src/types.ts"
      via: "BlogPost.relatedPostSlugs field access"
      pattern: "fetchedPost\\.relatedPostSlugs"
---

<objective>
Extend the BlogPost interface with missing fields and fix all as-any casts in consumer files that depend on those fields. Remove debug console.log from BlogHome.tsx and delete the dead src/server/api.ts file.

Purpose: Eliminate type safety gaps in the BlogPost type and its consumers, remove dead code and debug logging.
Output: Clean types.ts, blogUtils.ts, BlogPost.tsx, BlogHome.tsx; deleted api.ts.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/types.ts
@src/blog/utils/blogUtils.ts
@src/blog/BlogPost.tsx
@src/blog/BlogHome.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BlogPost type and fix as-any in consumer files</name>
  <files>src/types.ts, src/blog/utils/blogUtils.ts, src/blog/BlogPost.tsx</files>
  <action>
  **In src/types.ts**, add two optional fields to the BlogPost interface, after the existing `sourceUrl?: string;` line:

  ```typescript
  primaryTag?: string;
  relatedPostSlugs?: string[];
  ```

  **In src/blog/utils/blogUtils.ts** (line ~184), replace:
  ```typescript
  const primaryTag = (currentPost as any).primaryTag as string | undefined;
  ```
  with:
  ```typescript
  const primaryTag = currentPost.primaryTag;
  ```
  The field is now on BlogPost, and it's already `string | undefined` (optional), so no additional cast needed.

  **In src/blog/BlogPost.tsx**, fix three as-any casts:

  1. Line ~55 — replace `(fetchedPost as any).relatedPostSlugs` with `fetchedPost.relatedPostSlugs` (field now exists on BlogPost).

  2. Line ~300 — replace `(post as any).amazonProducts` with `post.amazonProducts` (amazonProducts is ALREADY on BlogPost interface — the cast was always unnecessary).

  3. Line ~302 — replace `(post as any).amazonProducts` with `post.amazonProducts` (same fix, second occurrence on the next line).

  NOTE: BlogPost.tsx imports from `../types.d` — do NOT change this import path, it works as-is.
  </action>
  <verify>
  Run `npx tsc --noEmit` and confirm no type errors in types.ts, blogUtils.ts, or BlogPost.tsx.
  Run a grep: `rg "as any" src/blog/utils/blogUtils.ts src/blog/BlogPost.tsx` — should return zero matches.
  </verify>
  <done>BlogPost interface has primaryTag and relatedPostSlugs fields. Zero as-any casts remain in blogUtils.ts and BlogPost.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Remove debug console.log and delete dead api.ts</name>
  <files>src/blog/BlogHome.tsx, src/server/api.ts</files>
  <action>
  **In src/blog/BlogHome.tsx**, remove two debug console.log statements:

  1. Line ~73: Delete the entire line `console.log("Total posts loaded:", allPosts.length);`
  2. Line ~120: Delete the entire line `console.log("After filtering - Posts to display:", result.length);`

  Keep ALL console.error calls — those are for real errors.

  **Delete src/server/api.ts entirely.** This file is confirmed dead code:
  - Nothing in the codebase imports it (verified via grep for `server/api`)
  - It has @ts-nocheck and 3 eslint-disable directives
  - It imports from `../blog/productData` which Phase 02 already deleted
  - The active API lives in server.js (development-only Express server)
  </action>
  <verify>
  Confirm file deleted: `ls src/server/api.ts` should fail (file not found).
  Run: `rg "console\.log" src/blog/BlogHome.tsx` — should return zero matches.
  Run: `rg "server/api" src/` — should return zero matches (nothing imported it).
  </verify>
  <done>Zero debug console.log in BlogHome.tsx. src/server/api.ts no longer exists.</done>
</task>

</tasks>

<verification>
- `rg "as any" src/blog/utils/blogUtils.ts src/blog/BlogPost.tsx` returns zero matches
- `rg "console\.log" src/blog/BlogHome.tsx` returns zero matches
- `ls src/server/api.ts` fails (file deleted)
- `npx tsc --noEmit` passes for modified files (or whole project if feasible)
</verification>

<success_criteria>
- BlogPost interface extended with primaryTag?: string and relatedPostSlugs?: string[]
- Zero as-any casts in blogUtils.ts (was 1) and BlogPost.tsx (was 3)
- Zero debug console.log in BlogHome.tsx (was 2)
- src/server/api.ts deleted (was 719 lines of dead code with @ts-nocheck)
</success_criteria>

<output>
After completion, create `.planning/phases/03-type-safety-code-quality/03-01-SUMMARY.md`
</output>
