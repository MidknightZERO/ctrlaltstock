---
phase: 03-type-safety-code-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blog/utils/markdownUtils.ts
  - src/blog/components/BlockEditor.tsx
autonomous: true

must_haves:
  truths:
    - "Zero as-any casts in markdownUtils.ts"
    - "Zero as-any casts in BlockEditor.tsx"
    - "ContentBlock properly imported in markdownUtils.ts"
    - "No debug console.log in markdownUtils.ts"
  artifacts:
    - path: "src/blog/utils/markdownUtils.ts"
      provides: "Type-safe markdown generation"
      contains: "import { BlogPost, ContentBlock"
    - path: "src/blog/components/BlockEditor.tsx"
      provides: "Type-safe block editor"
  key_links:
    - from: "src/blog/utils/markdownUtils.ts"
      to: "src/types.ts"
      via: "ContentBlock import for discriminated union narrowing"
      pattern: "import.*ContentBlock.*from"
    - from: "src/blog/components/BlockEditor.tsx"
      to: "src/types.ts"
      via: "ContentBlock already imported, switch narrows union"
      pattern: "switch \\(block\\.type\\)"
---

<objective>
Remove all as-any casts from markdownUtils.ts and BlockEditor.tsx by leveraging TypeScript's discriminated union narrowing on ContentBlock. Fix the ContentBlock import in markdownUtils.ts, type the currentBlock variable, and remove debug logging.

Purpose: Eliminate the remaining as-any casts in the codebase by using the type system correctly — the ContentBlock discriminated union already narrows to specific block types in switch cases.
Output: Clean markdownUtils.ts and BlockEditor.tsx with zero as-any casts.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/types.ts
@src/blog/utils/markdownUtils.ts
@src/blog/components/BlockEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix markdownUtils.ts type safety and cleanup</name>
  <files>src/blog/utils/markdownUtils.ts</files>
  <action>
  **Fix import** (line 1): Change `import { BlogPost } from '../../types';` to:
  ```typescript
  import { BlogPost, ContentBlock } from '../../types';
  ```
  ContentBlock is used throughout the file but was never imported (it compiled because the type was inferred from BlogPost.contentBlocks).

  **Fix currentBlock typing** (line ~142): Change `let currentBlock: any = null;` to:
  ```typescript
  let currentBlock: ContentBlock | null = null;
  ```

  **Remove 4 as-any casts in generateMarkdownFromBlocks function** (~lines 391, 400, 408, 418). In each switch case, the `block` parameter (typed as ContentBlock) is already narrowed by the switch on `block.type`. Remove the intermediate `as any` variable and use `block` directly:

  1. **case 'headline'** (~line 391): Remove `const headlineBlock = block as any;`. Replace all `headlineBlock.` references with `block.` — after `case 'headline'`, block is narrowed to HeadlineBlock which has `level` and `content`.

  2. **case 'image'** (~line 400): Remove `const imageBlock = block as any;`. Replace all `imageBlock.` references with `block.` — after `case 'image'`, block is narrowed to ImageBlock which has `url`, `alt`, `caption`.

  3. **case 'product'** (~line 408): Remove `const productBlock = block as any;`. Replace all `productBlock.` references with `block.` — after `case 'product'`, block is narrowed to ProductBlock which has `productId`.

  4. **case 'snippet'** (~line 418): Remove `const snippetBlock = block as any;`. Replace all `snippetBlock.` references with `block.` — after `case 'snippet'`, block is narrowed to SnippetBlock which has `language` and `content`.

  **Remove debug console.log** (line ~42): Delete the line `console.log(\`Parsing post: ${slug}\`);`
  </action>
  <verify>
  Run: `rg "as any" src/blog/utils/markdownUtils.ts` — should return zero matches.
  Run: `rg "console\.log" src/blog/utils/markdownUtils.ts` — should return zero matches.
  Run: `npx tsc --noEmit` and confirm no type errors in markdownUtils.ts.
  </verify>
  <done>markdownUtils.ts has zero as-any casts, ContentBlock properly imported, currentBlock typed as ContentBlock | null, debug console.log removed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix BlockEditor.tsx discriminated union narrowing</name>
  <files>src/blog/components/BlockEditor.tsx</files>
  <action>
  BlockEditor.tsx already imports `ContentBlock` from `../../types` (line 4). The file has 10 as-any casts across two functions: `renderBlockEditor` and `blocksToHtml`. Both use `switch (block.type)` where `block` is typed as `ContentBlock`. TypeScript narrows ContentBlock in each case branch, making all casts unnecessary.

  **In renderBlockEditor function** (~lines 248-420), remove 5 as-any casts:

  1. **case 'headline'** (~line 248): Remove `const headlineBlock = block as any;`. Replace all `headlineBlock.` with `block.` (HeadlineBlock has `level`, `content`).

  2. **case 'image'** (~line 288): Remove `const imageBlock = block as any;`. Replace all `imageBlock.` with `block.` (ImageBlock has `url`, `alt`, `caption`).

  3. **case 'product'** (~line 331): Remove `const productBlock = block as any;`. Replace all `productBlock.` with `block.` (ProductBlock has `productId`, `productName`, `imageUrl`).

  4. **case 'divider'** (~line 354): Remove `const dividerBlock = block as any;`. Replace all `dividerBlock.` with `block.` (DividerBlock has `style`).

  5. **case 'snippet'** (~line 385): Remove `const snippetBlock = block as any;`. Replace all `snippetBlock.` with `block.` (SnippetBlock has `language`, `content`).

  **In blocksToHtml function** (~lines 619-665), remove 5 as-any casts using the exact same pattern:

  1. **case 'headline'** (~line 625): Remove `const headlineBlock = block as any;`, use `block.level`.
  2. **case 'image'** (~line 630): Remove `const imageBlock = block as any;`, use `block.url`, `block.alt`, `block.caption`.
  3. **case 'product'** (~line 638): Remove `const productBlock = block as any;`, use `block.productId`.
  4. **case 'divider'** (~line 655): Remove `const dividerBlock = block as any;`, use `block.style`.
  5. **case 'snippet'** (~line 660): Remove `const snippetBlock = block as any;`, use `block.language`, `block.content`.

  Do NOT change BlockEditor.tsx imports — it already has what it needs.
  </action>
  <verify>
  Run: `rg "as any" src/blog/components/BlockEditor.tsx` — should return zero matches.
  Run: `npx tsc --noEmit` and confirm no type errors in BlockEditor.tsx.
  </verify>
  <done>BlockEditor.tsx has zero as-any casts. All 10 removed, replaced with proper discriminated union narrowing.</done>
</task>

</tasks>

<verification>
- `rg "as any" src/blog/utils/markdownUtils.ts src/blog/components/BlockEditor.tsx` returns zero matches
- `rg "console\.log" src/blog/utils/markdownUtils.ts` returns zero matches
- `npx tsc --noEmit` passes for modified files
- ContentBlock is in the import statement of markdownUtils.ts
</verification>

<success_criteria>
- Zero as-any casts in markdownUtils.ts (was 4) and BlockEditor.tsx (was 10)
- ContentBlock imported in markdownUtils.ts (was missing)
- currentBlock typed as ContentBlock | null (was any)
- Debug console.log removed from markdownUtils.ts (was 1)
</success_criteria>

<output>
After completion, create `.planning/phases/03-type-safety-code-quality/03-02-SUMMARY.md`
</output>
