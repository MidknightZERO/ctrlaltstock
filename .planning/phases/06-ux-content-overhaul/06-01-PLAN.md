---
phase: 06-ux-content-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - index.html
  - src/components/Layout.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage has no Twitter/Latest Updates embed"
    - "Single 'Latest hardware news' post appears below How it works"
    - "No Share API or similar is auto-invoked (no scary browser prompts)"
    - "Privacy Policy link goes to the correct route"
  artifacts:
    - path: src/App.tsx
      provides: "Landing sections including latest post and no Twitter embed"
    - path: index.html
      provides: "No Twitter widgets script when embed removed"
  key_links:
    - from: "App.tsx"
      to: "getAllPosts"
      via: "fetch latest post for Latest hardware news"
      pattern: "getAllPosts|blogPosts"
    - from: "Footer / Layout"
      to: "/privacy-policy"
      via: "Link to=/privacy-policy"
      pattern: "privacy-policy"
---

<objective>
Remove the broken "Latest Updates" Twitter feed from the homepage, add a "Latest hardware news" section featuring the single most recent blog post directly below "How it works", and ensure no browser prompts (e.g. Share API) are triggered. Fix Privacy Policy link to match the route defined in main.tsx.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/App.tsx
@src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Remove Twitter embed and add Latest hardware news section</name>
  <files>src/App.tsx, index.html</files>
  <action>
1. In src/App.tsx: Remove the entire "Latest Updates" section (the block that contains the twitter-timeline anchor and "Tweets by CtrlAltStock"). Do not add any replacement that loads Twitter widgets or invokes navigator.share / Web Share API.
2. In src/App.tsx: Insert a new section "Latest hardware news" directly after "How it works" and before "Now Tracking". This section must:
   - Use the existing blogPosts state (already fetched via getAllPosts, sorted by publishedDate descending).
   - Display exactly the single most recent post: title, excerpt (or first ~150 chars of content), cover image if present, and a "Read more" link to /blog/{slug}. Style consistently with the rest of the page (gray-800/gray-900, lime accent).
   - If blogPosts is empty, show a short message like "No posts yet" with a link to /blog.
3. In index.html: Remove the script tag that loads platform.twitter.com/widgets.js (to avoid unnecessary requests and any related prompts).
  </action>
  <verify>
- npm run build succeeds.
- In browser at /: no "Latest Updates" or Twitter timeline; "Latest hardware news" section appears below How it works with one post (or "No posts yet"); no console errors about twttr or share.
- index.html does not contain "twitter" or "widgets.js".
  </verify>
  <done>
Twitter embed removed; Latest hardware news shows single most recent post below How it works; Twitter script removed from index.html.
  </done>
</task>

<task type="auto">
  <name>Fix Privacy Policy link and verify no Share API</name>
  <files>src/App.tsx, src/components/Layout.tsx</files>
  <action>
1. In src/App.tsx: Change the footer link for Privacy Policy from to="/privacy" to to="/privacy-policy" (main.tsx defines the route as /privacy-policy).
2. In src/components/Layout.tsx: If it contains a link to /privacy for Privacy Policy, change it to /privacy-policy.
3. Grep the repo for navigator.share, share\(, or Web Share API usage on the landing or blog post page. If any auto-invocation (e.g. on load or without user click) exists, remove or guard it so the Share API is only called in response to an explicit user action (e.g. "Share" button click). Document in a short comment that we do not auto-invoke Share to avoid browser prompts.
  </action>
  <verify>
- Click "Privacy Policy" in footer: navigates to /privacy-policy and shows the privacy page.
- No code path calls navigator.share or similar without a direct user click.
  </verify>
  <done>
Privacy link points to /privacy-policy; no auto-invocation of Share or similar APIs.
  </done>
</task>

</tasks>

<verification>
- Run npm run build; run npm run dev and open /. Confirm no Twitter section, "Latest hardware news" shows one post, Privacy link works, no console errors.
</verification>

<success_criteria>
- Homepage has no Twitter embed. Latest hardware news section shows the most recent blog post. Privacy Policy link is /privacy-policy. No scary browser prompts from Share or Twitter.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-content-overhaul/06-01-SUMMARY.md`
</output>
