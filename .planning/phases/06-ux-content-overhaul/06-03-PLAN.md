---
phase: 06-ux-content-overhaul
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/InfiniteScroll.tsx
autonomous: true

must_haves:
  truths:
    - "Now Tracking section shows current-gen GPUs, CPUs, consoles, and RAM"
    - "Each item has image, text, and click-through to Discord invite"
    - "All product links use the same general Discord invite (no product-specific URLs for now)"
  artifacts:
    - path: src/components/InfiniteScroll.tsx
      provides: "Now Tracking product strip with images and Discord CTA"
  key_links:
    - from: "InfiniteScroll.tsx"
      to: "DISCORD_INVITE or constant"
      via: "Each card links to Discord"
      pattern: "discord\.gg|DISCORD"
---

<objective>
Update the "Now Tracking" section with current-generation hardware: GPUs, CPUs, consoles, and RAM. Each item must show an image, product name (and optional short line), and a click-through that links to the general Discord invite. Use a single Discord invite constant for all cards.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/components/InfiniteScroll.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Refresh Now Tracking product list and Discord links</name>
  <files>src/components/InfiniteScroll.tsx</files>
  <action>
1. Update the PRODUCTS array in InfiniteScroll.tsx to represent current-gen (2025–2026) hardware. Include at least: 2–3 GPUs (e.g. RTX 5090/5080, RX 9000 or current AMD), 1–2 CPUs (e.g. Ryzen 9000, Intel 15th gen or current), 1–2 consoles (e.g. PS5 Pro, Xbox Series X, or current), and 1 RAM or memory product. Use stable, high-quality image URLs (Unsplash or similar, or existing project images). Each product object must have: name, image (URL), and an optional short description line.
2. Make each card a link (or contain a clear CTA link) to the same Discord invite (e.g. https://discord.gg/MqqbyJJbvC or a constant shared with App.tsx). The entire card or a "Join to track" button should go to Discord. Do not link to Amazon or product pages; all click-through goes to Discord for now.
3. Keep the existing horizontal scroll / animate-scroll behavior and styling (gray-800 cards, hover states). Ensure the section title "Now Tracking" remains in App.tsx and is still visible above this component.
  </action>
  <verify>
- npm run build succeeds. On homepage, "Now Tracking" shows the new product set; each card has image + text; clicking a card or CTA opens Discord invite in a new tab.
  </verify>
  <done>
Now Tracking displays current-gen GPUs, CPUs, consoles, and RAM; every card links to the general Discord invite.
  </done>
</task>

</tasks>

<verification>
- Manual test: scroll to Now Tracking, confirm product mix and that all links go to Discord.
</verification>

<success_criteria>
- Now Tracking section shows current-gen hardware with images and Discord-only links.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-content-overhaul/06-03-SUMMARY.md`
</output>
