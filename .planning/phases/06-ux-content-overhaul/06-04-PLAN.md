---
phase: 06-ux-content-overhaul
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src/data/communityTestimonials.ts
  - src/components/CommunityTestimonialsScroller.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Testimonials are real quotes from 2021/2022 Reddit or forums mentioning CtrlAltStock"
    - "Each testimonial has username, direct quote, source URL, and optional platform"
    - "Two opposite-direction scrollers display the testimonials"
    - "No placeholder or fake testimonials remain"
  artifacts:
    - path: src/data/communityTestimonials.ts
      provides: "Curated testimonials with quote, username, sourceUrl"
    - path: src/components/CommunityTestimonialsScroller.tsx
      provides: "Dual opposite-direction scrolling testimonials UI"
  key_links:
    - from: "App.tsx"
      to: "CommunityTestimonialsScroller"
      via: "Replace TESTIMONIALS grid with scroller"
      pattern: "CommunityTestimonialsScroller"
---

<objective>
Replace placeholder testimonials with real community success stories from 2021/2022 (Reddit, forums) where people mention CtrlAltStock or getting GPUs/consoles with the community. Display them in dual opposite-direction scrollers with direct quotes, usernames, sources, and clickable links. Research must use WebSearch to find real, attributable posts—do not invent quotes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Research and curate real testimonials</name>
  <files>src/data/communityTestimonials.ts</files>
  <action>
Use WebSearch to find public posts from 2021–2022 (Reddit, forums like r/buildapc, r/nvidia, r/AMD, Discord-related forums, or tech deal communities) where users mention CtrlAltStock, Ctrl Alt Stock, or describe getting GPUs/consoles with the help of the community or stock alerts. Collect: exact quote (short, 1–2 sentences), username, platform (Reddit, etc.), and direct URL to the post. Do NOT invent or paraphrase; only use verifiable, public posts. If fewer than 4–6 are found, document that and use what exists; prefer quality over quantity. Create src/data/communityTestimonials.ts exporting an array of objects: { quote: string, username: string, sourceUrl: string, platform?: string }. Minimum 3 testimonials; aim for 5–8.
  </action>
  <verify>
- File exists; each entry has quote, username, sourceUrl. No placeholder text like "TechHunter" or "Finally got my hands on...". Run a quick WebSearch to spot-check one sourceUrl is real.
  </verify>
  <done>
communityTestimonials.ts contains only real, sourced testimonials with links.
  </done>
</task>

<task type="auto">
  <name>Build dual opposite-direction scroller and integrate</name>
  <files>src/components/CommunityTestimonialsScroller.tsx, src/App.tsx</files>
  <action>
1. Create CommunityTestimonialsScroller.tsx that:
   - Imports testimonials from src/data/communityTestimonials.ts.
   - Renders two horizontal strips (or one strip with two rows): one scrolling left, one scrolling right (CSS animation only, infinite loop). Each strip shows testimonial cards (quote, username, "Source" link to sourceUrl). Cards should be compact (e.g. max-width 320px), readable, and styled to match the site (gray-800, lime accent for links).
   - Use duplicate content in each strip for seamless loop (e.g. [...items, ...items]).
2. In App.tsx: Remove the existing "Community Success Stories" grid that uses TESTIMONIALS. Replace it with CommunityTestimonialsScroller. Remove the TESTIMONIALS constant from App.tsx if no longer used.
  </action>
  <verify>
- npm run build succeeds. Homepage "Community Success Stories" section shows two scrolling rows (opposite directions) with real quotes and source links. No placeholder testimonials.
  </verify>
  <done>
Dual scroller displays real testimonials with attribution and links; placeholders removed.
  </done>
</task>

</tasks>

<verification>
- Manual: scroll to testimonials; confirm both rows animate in opposite directions; click a source link and confirm it goes to the real post.
</verification>

<success_criteria>
- Real, attributed community testimonials in dual opposite-direction scrollers; no fake data.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-content-overhaul/06-04-SUMMARY.md`
</output>

<notes>
WebSearch is required for Task 1 to find real Reddit/forum posts. Do not guess or fabricate quotes.
</notes>
