---
phase: 06-ux-content-overhaul
plan: 06
type: execute
wave: 3
depends_on: []
files_modified:
  - src/blog/BlogPost.tsx
  - src/blog/BlogHome.tsx
  - scripts/build-blog.js
  - public/Logo.png
autonomous: true

must_haves:
  truths:
    - "Blog posts show CAS logo as author avatar when author is team/CtrlAltStock"
    - "Fallback for missing author.avatar uses CAS logo for team posts"
  artifacts:
    - path: src/blog/BlogPost.tsx
      provides: "Author avatar rendering with CAS logo fallback"
    - path: src/blog/BlogHome.tsx
      provides: "Same avatar logic in post cards"
  key_links:
    - from: "BlogPost.tsx / BlogHome.tsx"
      to: "Logo or default team avatar"
      via: "author.avatar or default for team"
      pattern: "author\.avatar|Logo"
---

<objective>
Use the CAS (CtrlAltStock) logo as the blog author avatar for team posts. The logo asset is referenced in the project (e.g. Logo.png in public or src/images); use it when author.avatar is missing or when author is the team so all team posts show a consistent brand avatar.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/blog/BlogPost.tsx
@src/blog/BlogHome.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Use CAS logo as default team avatar in blog UI</name>
  <files>src/blog/BlogPost.tsx, src/blog/BlogHome.tsx</files>
  <action>
1. Determine the CAS logo path: use /Logo.png (public) or import from src/images/Logo.png if present. Prefer public path so no build change is required.
2. In BlogPost.tsx where author avatar is rendered: if post.author.avatar is missing or empty, or if author.name is empty/unknown, use the CAS logo URL (e.g. /Logo.png) for the img src. Alt text should remain author name or "CtrlAltStock team".
3. In BlogHome.tsx in the post card author block: apply the same fallback—when post.author.avatar is missing or empty, use the CAS logo. Ensure img onError does not break layout (optional: fallback to logo on error).
4. Do not change markdown or build script for this task unless needed to set a default author; focus on frontend display only.
  </action>
  <verify>
- npm run build succeeds. Open a blog post with no author avatar (or empty author): avatar shows Logo.png. Blog listing cards show the same for posts without avatar.
  </verify>
  <done>
Blog post and listing show CAS logo when author avatar is missing or for team.
  </done>
</task>

<task type="auto">
  <name>Default author in build script for new/legacy posts</name>
  <files>scripts/build-blog.js</files>
  <action>
In build-blog.js, when processing author from frontmatter: if author.name is empty or missing after parsing, set a default: name "CtrlAltStock", avatar "/Logo.png", bio optional (e.g. "Community-run stock alerts and hardware news"). This ensures generated blog-posts.json has a consistent team author for posts that do not specify one, so the frontend fallback from 06-06 Task 1 displays the logo without relying only on empty avatar.
  </action>
  <verify>
- Run node scripts/build-blog.js (or npm run build if it runs the script). Inspect public/blog-posts.json: posts with no author have name "CtrlAltStock" and avatar "/Logo.png".
  </verify>
  <done>
Build script assigns default team author (name + /Logo.png) when frontmatter author is missing.
  </done>
</task>

</tasks>

<verification>
- View blog listing and 2–3 posts; team/default author shows CAS logo.
</verification>

<success_criteria>
- CAS logo appears as author avatar on blog posts and listing when author is team or unspecified.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-content-overhaul/06-06-SUMMARY.md`
</output>
