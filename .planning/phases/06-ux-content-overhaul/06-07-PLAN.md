---
phase: 06-ux-content-overhaul
plan: 07
type: execute
wave: 3
depends_on: []
files_modified:
  - src/blog/BlogHome.tsx
  - src/blog/data/tagHierarchy.ts
  - scripts/build-blog.js
  - src/blog/utils/blogUtils.ts
autonomous: true

must_haves:
  truths:
    - "Blog tags are cleaned (no word-salad or irrelevant tags)"
    - "Posts are ordered by publishedDate descending (newest first)"
    - "9 posts per page instead of 6"
  artifacts:
    - path: src/blog/BlogHome.tsx
      provides: "Pagination with POSTS_PER_PAGE = 9"
    - path: scripts/build-blog.js
      provides: "Sort by date when writing blog-posts.json"
  key_links:
    - from: "blogUtils.getAllPosts / BlogHome"
      to: "sorted by publishedDate"
      via: "sort before setPosts / filteredPosts"
      pattern: "sort.*publishedDate|getTime"
---

<objective>
Clean up blog tags (remove word-salad and noise), fix latest-post ordering so the list is consistently by date (newest first), and show 9 posts per page instead of 6. Tag cleanup can be via allowlist in tagHierarchy or filtering in the UI; ordering must be enforced in both build script and frontend.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/blog/BlogHome.tsx
@src/blog/data/tagHierarchy.ts
@scripts/build-blog.js
</context>

<tasks>

<task type="auto">
  <name>Fix post ordering and 9 posts per page</name>
  <files>src/blog/BlogHome.tsx, scripts/build-blog.js</files>
  <action>
1. In BlogHome.tsx: Change POSTS_PER_PAGE from 6 to 9. Ensure the initial sort of posts (when loading from getAllPosts) is by publishedDate descending: sort((a,b) => new Date(b.publishedDate).getTime() - new Date(a.publishedDate).getTime()). If there is any other code path that sets posts or filteredPosts, ensure it preserves this order (e.g. do not sort by anything else by default).
2. In scripts/build-blog.js: The build already sorts by date when writing blog-posts.json. Confirm the sort is newest-first (b.publishedDate - a.publishedDate). If the JSON is consumed as-is by the API or static file, the frontend must also sort on load (in case of merge/cache); keep the sort in BlogHome so ordering is guaranteed regardless of source.
  </action>
  <verify>
- Rebuild blog (npm run build or node scripts/build-blog.js). Open /blog: first page shows 9 posts; order is newest first (check dates). Page 2 shows next 9.
  </verify>
  <done>
9 posts per page; post order is newest first in build and on /blog.
  </done>
</task>

<task type="auto">
  <name>Clean up blog tags (remove word-salad)</name>
  <files>src/blog/data/tagHierarchy.ts, src/blog/BlogHome.tsx</files>
  <action>
1. Define an allowlist of displayable tags: use MAIN_GROUPS and ALL_SUBCATEGORIES from tagHierarchy.ts, plus a small set of known good tags (e.g. GPU, CPU, PlayStation, Xbox, Deals, Guides, NVIDIA, AMD, etc.). In BlogHome (or in blogUtils), when building displayTags or when passing tags to the UI, filter the raw tags from posts so that only tags that appear in the allowlist (or that match a known pattern, e.g. 2–30 chars, no random phrases) are shown. Alternatively: add a DISPLAY_TAGS allowlist in tagHierarchy.ts and filter tags in BlogHome to only show tags in that list; tags not in the list are hidden from the Topics filter but posts still filter by tag if URL has ?tag=.
2. Ensure no crash when a post has tags that are long strings or "word salad"; normalize or exclude them from the topic pills. Document in a short comment that we filter to hierarchy + allowlist to avoid word-salad.
  </action>
  <verify>
- Open /blog: Topics section shows only coherent tags (Hardware, GPU, Deals, etc.), not long or nonsensical phrases. Selecting a tag still filters posts. npm run build succeeds.
  </verify>
  <done>
Tags shown in UI are limited to allowlist/hierarchy; word-salad tags are hidden from display.
  </done>
</task>

</tasks>

<verification>
- Check /blog: 9 posts per page, newest first; tags are clean and filterable.
</verification>

<success_criteria>
- 9 posts per page; strict date order (newest first); tags cleaned for display.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-content-overhaul/06-07-SUMMARY.md`
</output>
