---
phase: 06-ux-content-overhaul
plan: 08
type: execute
wave: 3
depends_on: ["06-07"]
files_modified:
  - src/blog/data/pillarPosts.ts
  - src/blog/BlogHome.tsx
autonomous: true

must_haves:
  truths:
    - "Blog homepage has a prominent hero featuring rotating pillar posts"
    - "Pillar posts are defined by slug and get priority in the hero"
    - "Hero links to pillar posts with strong internal linking"
  artifacts:
    - path: src/blog/data/pillarPosts.ts
      provides: "List of pillar post slugs (e.g. best-budget-gpus-2026)"
    - path: src/blog/BlogHome.tsx
      provides: "Hero section with rotating pillar content"
  key_links:
    - from: "BlogHome.tsx"
      to: "pillarPosts"
      via: "Hero uses pillar slugs to pick featured posts"
      pattern: "pillarPosts|pillar"
---

<objective>
Redesign the blog homepage hero into a big rotating pillar-post hero (e.g. "Best budget GPUs in 2026"). Define a small set of pillar topic slugs; the hero rotates through posts that match those slugs (or the first N posts if pillar posts do not exist yet). Strong internal linking: hero CTA and cards link to pillar or featured posts.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/blog/BlogHome.tsx
@src/blog/components/BlogFeaturedSlideshow.tsx
</context>

<tasks>

<task type="auto">
  <name>Define pillar posts and hero data</name>
  <files>src/blog/data/pillarPosts.ts</files>
  <action>
Create src/blog/data/pillarPosts.ts that exports:
- PILLAR_SLUGS: string[] — e.g. ["best-budget-gpus-2026", "best-cpus-for-gaming-2026", "ps5-vs-xbox-series-x"] (use slugs that match existing or planned pillar posts; can be updated later).
- Optional: a function getPillarPosts(posts: BlogPost[]) that returns posts whose slug is in PILLAR_SLUGS, in order of PILLAR_SLUGS, then fills remaining slots (e.g. 5) with newest posts not in the list. This drives the hero rotation.
Use 3–5 pillar slugs; prefer slugs that already exist in the repo so the hero has content. If none exist, use placeholder slugs and document that pillar content will be added in a later plan.
  </action>
  <verify>
- File exists; PILLAR_SLUGS is an array of strings; getPillarPosts (if implemented) returns a subset of posts with pillar first.
  </verify>
  <done>
pillarPosts.ts defines pillar slugs and optional helper for hero ordering.
  </done>
</task>

<task type="auto">
  <name>Blog hero as rotating pillar section</name>
  <files>src/blog/BlogHome.tsx</files>
  <action>
1. In BlogHome, compute hero posts: use getPillarPosts(posts) if available, else use posts.slice(0, FEATURED_COUNT) sorted by date. So pillar posts appear first in the hero when they exist.
2. Replace or repurpose the current "Featured" slideshow so the hero is prominent (e.g. larger first slide, title like "Featured guides" or "Start here"). Use the existing BlogFeaturedSlideshow with the hero posts list. Ensure each slide links to the post (already does) and consider adding a short "Pillar" or "Guide" label for pillar slugs so internal linking is clear.
3. No new dependencies; keep existing slideshow component. Ensure mobile layout still works.
  </action>
  <verify>
- Open /blog: hero/slideshow shows posts; when pillar slugs exist in the post list, they appear first in the rotation. Links go to correct /blog/{slug}. npm run build succeeds.
  </verify>
  <done>
Blog homepage hero rotates through pillar posts first, then newest; strong internal links.
  </done>
</task>

</tasks>

<verification>
- Manual: /blog hero shows rotating featured/pillar posts; links work.
</verification>

<success_criteria>
- Pillar slugs defined; blog hero prioritizes pillar posts and links to them.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-content-overhaul/06-08-SUMMARY.md`
</output>
