---
phase: 06-ux-content-overhaul
plan: 10
type: execute
wave: 4
depends_on: []
files_modified:
  - bot/stock_images.json
  - bot/used_images.json
  - src/blog/data/stockImages.ts
  - bot/publisher.py
  - directives/scrape_website.md
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Bot uses a larger, curated stock image pool to reduce reuse"
    - "Image selection avoids recently used images when possible"
  artifacts:
    - path: bot/stock_images.json
      provides: "Curated image URLs by category"
    - path: bot/used_images.json
      provides: "Tracking of recently used images"
  key_links:
    - from: "bot image selection"
      to: "stock_images.json / used_images"
      via: "Pick from pool; record usage"
      pattern: "stock_images|used_images"
---

<objective>
Reduce image reuse across posts by growing a curated stock image catalogue and ensuring the bot prefers images that have not been used recently. Update bot logic and/or directives so new posts get more varied imagery.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@bot/stock_images.json
@bot/used_images.json
</context>

<tasks>

<task type="auto">
  <name>Expand stock image catalogue and usage tracking</name>
  <files>bot/stock_images.json, bot/used_images.json, src/blog/data/stockImages.ts</files>
  <action>
1. Read current bot/stock_images.json and bot/used_images.json (create if missing). Structure: stock_images.json should list image URLs by category (e.g. gpu, cpu, console, deals, general) so the bot can pick by article topic. Add 15–30 new high-quality, royalty-free image URLs (Unsplash, Pexels, or existing project assets) across categories to reduce reuse. Ensure URLs are stable (no temporary links).
2. Ensure used_images.json (or equivalent) records recent image usage (e.g. image URL + slug or date). Bot code that assigns images to posts should prefer images not in the last N uses (e.g. 10–20). If no such logic exists, add it in the script that sets coverImage or images in the draft (e.g. publisher or ai_refiner).
3. If src/blog/data/stockImages.ts is used by the frontend, keep it in sync or document that bot uses bot/stock_images.json as source of truth for new posts; frontend can have its own copy for fallbacks.
  </action>
  <verify>
- stock_images.json has multiple images per category; used_images structure is clear. Grep bot for where coverImage/images are set; confirm it uses stock_images and avoids recently used.
  </verify>
  <done>
Larger curated catalogue; bot prefers unused or less-recently-used images.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify image variety and directive update</name>
  <what-built>Expanded stock image catalogue and bot logic to reduce reuse</what-built>
  <how-to-verify>
1. Run the bot pipeline (or the step that assigns images) for a new draft and confirm it picks from stock_images and records usage.
2. Optionally run twice with similar topics and confirm different images are chosen when possible.
3. If directives/scrape_website.md or a bot directive describes image sourcing, update it to mention the curated catalogue and "prefer least-recently-used" so future runs stay consistent.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Bot run uses catalogue; usage is tracked; directive updated. Human confirms no regression.
</verification>

<success_criteria>
- Curated stock image catalogue expanded; bot reduces reuse via tracking; directive reflects process.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-content-overhaul/06-10-SUMMARY.md`
</output>
