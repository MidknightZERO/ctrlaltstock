---
phase: 06-ux-content-overhaul
plan: 11
type: execute
wave: 5
depends_on: ["06-08"]
files_modified:
  - src/blog/data/pillarPosts.ts
  - bot/ai_editor.py
  - directives/
autonomous: true

must_haves:
  truths:
    - "Pillar topics are defined and most internal links point at pillar posts"
    - "Bot or editor adds relatedPostSlugs that prefer pillar slugs when relevant"
  artifacts:
    - path: src/blog/data/pillarPosts.ts
      provides: "PILLAR_SLUGS and optional pillar metadata"
    - path: bot/ai_editor.py
      provides: "Internal linking logic that prefers pillars"
  key_links:
    - from: "ai_editor / linking step"
      to: "pillar slugs"
      via: "Suggest relatedPostSlugs with pillar first"
      pattern: "relatedPostSlugs|pillar"
---

<objective>
Define pillar topics clearly and ensure most internal links in the blog point at pillar posts to signal importance to Google. Update the bot's internal-linking step (e.g. ai_editor) to prefer pillar slugs when suggesting relatedPostSlugs, and document pillar strategy in a directive.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/blog/data/pillarPosts.ts
@bot/ai_editor.py
</context>

<tasks>

<task type="auto">
  <name>Define pillar topics and pillar-first linking</name>
  <files>src/blog/data/pillarPosts.ts, bot/ai_editor.py</files>
  <action>
1. In pillarPosts.ts (from 06-08): Ensure PILLAR_SLUGS is the single source of truth. Add a short comment or export PILLAR_TOPICS: { slug: string, title: string }[] for display (e.g. "Best budget GPUs 2026"). Ensure at least 3 pillar slugs are defined; use existing post slugs where they fit (e.g. buying guides, roundups).
2. In bot/ai_editor.py: Locate where relatedPostSlugs or internal links are generated. When suggesting related posts for a new or updated article, prefer posts whose slug is in PILLAR_SLUGS (load from pillarPosts.ts or a shared JSON/list the bot can read). So when the AI suggests 3–5 related posts, the first 1–2 should be pillar posts if they match the topic; then fill with other relevant posts. If the bot cannot import TypeScript, add a small JSON file (e.g. public/pillar-slugs.json) generated from PILLAR_SLUGS and have the bot read that, or pass pillar slugs via env/config.
  </action>
  <verify>
- pillarPosts.ts exports PILLAR_SLUGS (and optional PILLAR_TOPICS). Bot reads pillar list and uses it when generating relatedPostSlugs. Dry-run or unit test: related suggestions include pillar slug when relevant.
  </verify>
  <done>
Pillar topics defined; bot internal linking prefers pillar posts in relatedPostSlugs.
  </done>
</task>

<task type="auto">
  <name>Document pillar strategy in directive</name>
  <files>directives/internal_linking.md</files>
  <action>
Create or update a directive (e.g. directives/internal_linking.md or a section in an existing content directive) that describes: (1) Pillar posts are the main guides (e.g. "Best budget GPUs", "Best CPUs for gaming"); (2) New posts should link to at least 1–2 pillar posts when relevant; (3) relatedPostSlugs in frontmatter should list pillar slugs first when topic matches. Keep it short so future bot runs and manual edits follow the strategy.
  </action>
  <verify>
- File exists; content describes pillar-first linking and where to configure pillar slugs.
  </verify>
  <done>
Directive documents pillar strategy and internal linking rules.
  </done>
</task>

</tasks>

<verification>
- Pillar slugs used in linking; directive exists and is clear.
</verification>

<success_criteria>
- Pillar topics defined; most internal links point at pillars; directive in place.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-content-overhaul/06-11-SUMMARY.md`
</output>
