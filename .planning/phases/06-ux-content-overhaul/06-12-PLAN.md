---
phase: 06-ux-content-overhaul
plan: 12
type: execute
wave: 5
depends_on: ["06-11"]
files_modified:
  - bot/ai_writer.py
  - bot/ai_editor.py
  - directives/
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Pillar posts are listicles with many Amazon links and strong internal linking"
    - "Generation uses OpenRouter with guardrails so output is on-topic and structured"
  artifacts:
    - path: bot/ai_writer.py or dedicated pillar script
      provides: "Pillar post generation with listicle + links"
  key_links:
    - from: "pillar generator"
      to: "affiliate-products / amazon_linker"
      via: "Embed Amazon products and internal links"
      pattern: "amazon|relatedPostSlugs"
---

<objective>
Enable pillar post generation via OpenRouter: listicles (e.g. "Best budget GPUs in 2026") with many Amazon links and robust internal linking. Use strong prompts and guardrails so output is on-topic and does not hallucinate products. Optionally add a one-off script or extend the existing pipeline to generate one pillar post per pillar topic.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@bot/ai_writer.py
@bot/amazon_linker.py
</context>

<tasks>

<task type="auto">
  <name>Define pillar generation prompt and guardrails</name>
  <files>bot/ai_writer.py, directives/</files>
  <action>
1. Create a dedicated prompt (or a branch in ai_writer) for pillar posts. Input: pillar topic (e.g. "Best budget GPUs 2026"), PILLAR_SLUGS for internal linking, and optional list of product keywords from affiliate-products.json. Prompt must instruct the model to: output a listicle (numbered or bullet list of 5–10 items), each item with product name, short blurb, and placeholder for Amazon link; include an intro and conclusion; mention CtrlAltStock/Discord where natural; do not invent product names—use only provided keywords or generic descriptors ("best value GPU under £400") that can be matched later by amazon_linker.
2. Add guardrails: max word count per section, require at least N product placeholders, reject output that contains off-topic products (e.g. game titles in a GPU list). Document in a directive (e.g. directives/pillar_content.md) so runs are repeatable.
  </action>
  <verify>
- Run pillar generation for one topic (dry-run or test); output is a listicle with placeholders; no hallucinated product names. Directive exists.
  </verify>
  <done>
Pillar prompt and guardrails defined; output is listicle with link placeholders.
  </done>
</task>

<task type="auto">
  <name>Wire pillar output to amazon_linker and publish path</name>
  <files>bot/amazon_linker.py, bot/publisher.py</files>
  <action>
1. Ensure the pillar draft (from ai_writer) is passed through amazon_linker so placeholders are replaced with real affiliate links from affiliate-products.json (and fallback search where needed). Match placeholders by keyword/category (e.g. "best value GPU under £400" → GPU category products). Topic for pillar should be the pillar slug/category so linker picks relevant products.
2. Ensure the pillar post gets relatedPostSlugs pointing to other pillar slugs (from 06-11). Either ai_editor adds them or the pillar script sets them before publish.
3. Document in directive: how to run pillar generation (e.g. python bot/generate_pillar.py --topic best-budget-gpus-2026), and that generated markdown goes through normal build/publish flow.
  </action>
  <verify>
- End-to-end: generate one pillar post, run through linker and publish (or to file); post has multiple Amazon links and relatedPostSlugs to other pillars. Directive updated.
  </verify>
  <done>
Pillar posts get Amazon links and internal links; pipeline documented.
  </done>
</task>

</tasks>

<verification>
- One pillar post generated and wired; directive describes full flow.
</verification>

<success_criteria>
- Pillar listicles generatable with OpenRouter; many Amazon links; internal links to other pillars; guardrails in place.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ux-content-overhaul/06-12-SUMMARY.md`
</output>

<notes>
OpenRouter/API keys are assumed configured in .env; no user_setup for keys if already in use by bot.
</notes>
