---
phase: 07-image-handling-blog-order
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server.js
  - BLOG-README.md
autonomous: true

must_haves:
  truths:
    - "GET /api/posts returns posts ordered by publishedDate descending (newest first)"
    - "Documentation states that blog order is newest-first everywhere (build, API, frontend)"
  artifacts:
    - path: server.js
      provides: "API posts list sorted by date before response"
    - path: BLOG-README.md
      provides: "Section describing blog order (newest-first)"
  key_links:
    - from: "server.js GET /api/posts"
      to: "res.json(posts)"
      via: "sort by publishedDate descending before return"
      pattern: "sort.*publishedDate|getTime"
---

<objective>
Fix blog order when using the dev API so list is never backwards; document that blog order is newest-first everywhere so the "backwards" confusion is resolved.
Purpose: User thinks "the way they're being added might be backwards"; build and frontend already sort newest-first, but server.js returns posts in filesystem order. When using API (e.g. dev), order was wrong.
Output: API returns newest first; BLOG-README.md documents blog order.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-image-handling-blog-order/07-CONTEXT.md
@server.js
@BLOG-README.md
</context>

<tasks>

<task type="auto">
  <name>Sort GET /api/posts by publishedDate descending</name>
  <files>server.js</files>
  <action>
In server.js, in the GET /api/posts handler, after building the posts array from reading markdown files and before res.json(posts), sort the posts array by publishedDate descending (newest first). Use the same logic as build-blog.js and frontend: new Date(b.publishedDate).getTime() - new Date(a.publishedDate).getTime(). Handle missing or invalid dates by treating them as epoch 0 so they sort to the end. Do not change the response shape; only the order of the array.
  </action>
  <verify>
- Start server (node server.js or npm run dev:full), GET http://localhost:3001/api/posts — response array should have newest post first (compare publishedDate of first and last items). Frontend /blog when using API should show same order.
  </verify>
  <done>
GET /api/posts returns posts with newest first.
  </done>
</task>

<task type="auto">
  <name>Document blog order in BLOG-README.md</name>
  <files>BLOG-README.md</files>
  <action>
Add a short section to BLOG-README.md (e.g. under "How The Static Build Process Works" or a new "Blog order" subsection) that states: Blog order is newest-first everywhere. The build script (scripts/build-blog.js) writes blog-posts.json with posts sorted by publishedDate descending. The dev API (GET /api/posts) returns posts in the same order. All frontend list views (BlogHome, homepage, preview) sort by date newest-first. So the most recent post always appears at the top of lists and feeds.
  </action>
  <verify>
- Open BLOG-README.md and confirm the new section exists and clearly states newest-first order in build, API, and frontend.
  </verify>
  <done>
BLOG-README.md contains a "Blog order" (or equivalent) section documenting newest-first everywhere.
  </done>
</task>

</tasks>

<verification>
- API returns newest-first; documentation explains blog order.
</verification>

<success_criteria>
- GET /api/posts is sorted newest first; BLOG-README.md documents that blog order is newest-first across build, API, and frontend.
</success_criteria>

<output>
After completion, create `.planning/phases/07-image-handling-blog-order/07-02-SUMMARY.md`
</output>
